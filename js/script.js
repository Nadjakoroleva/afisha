window.onload = function() {
    let narrowDate = document.querySelector('.menu-date');

    narrowDate.addEventListener('click', function(evt) {
        event.preventDefault();
        console.log(1)
    })
}